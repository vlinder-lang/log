import vlinder.text

union Level {
    Debug
    Info
    Warning
    Error
    Critical
}

struct Record {
    level: Level
    message: text.String
}

typealias Logger = (Record) => ()

sub info(logger: Logger, message: text.String): () {
    logger(Record{level: Info, message: message})
}
