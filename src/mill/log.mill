import mill.text

union Level {
    Debug
    Info
    Warning
    Error
    Critical
}

struct Record {
    level: Level
    message: String
}

alias Logger = (Record) => ()

sub info(logger: Logger, message: text.String): () {
    logger(Record{level: Info, message: message})
}
